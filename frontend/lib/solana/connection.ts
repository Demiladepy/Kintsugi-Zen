import { clusterApiUrl } from "@solana/web3.js";

export const SOLANA_CLUSTER = "devnet";
export const SOLANA_ENDPOINT = clusterApiUrl(SOLANA_CLUSTER);
export const SOLSCAN_CLUSTER_QUERY = "cluster=devnet";
